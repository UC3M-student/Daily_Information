name: Daily Dashboard

on:
  schedule:
    - cron: '0 8 * * *'  # Every day at 08:00 UTC
  workflow_dispatch:      # Allows manual triggering

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout the repository
      - name: Checkout repo
        uses: actions/checkout@v3

      # 2️⃣ Set up Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 pandas

      # 4️⃣ Run dashboard script
      - name: Run dashboard script
        run: |
          python daily_dashboard.py

      # 5️⃣ Commit & push the HTML report
      - name: Commit & push report
        env:
          GITHUB_PAT: ${{ secrets.PAT_TOKEN }}  # Your Personal Access Token secret
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add docs/daily_report.html
          git commit -m "Update daily dashboard [skip ci]" || echo "No changes to commit"
          git push https://x-access-token:${GITHUB_PAT}@github.com/${{ github.repository }}.git HEAD:main
